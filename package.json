{
  "name": "Bide",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.2.1"
  },
  "dependencies": {
    "babel-standalone": "^6.12.0",
    "react": "^15.2.1",
    "react-codemirror": "^0.2.6",
    "react-dom": "^15.2.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp ./_blank.html ./build/_blank.html",
    "eject": "react-scripts eject",
    "deploy": "npm run build && now build"
  },
  "eslintConfig": {
    "extends": "./node_modules/react-scripts/config/eslint.js"
  }
}
